import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
	// App Component - re-usable and independent pieces of React code that comprise the User Interface
	<App />, document.getElementById('root')
);